#!/bin/sh

script_directory=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_directory=$(dirname -- "$script_directory")

cd "$project_directory" || exit 1
exec /opt/homebrew/bin/node --import tsx src/local/relay-mcp-server.ts
